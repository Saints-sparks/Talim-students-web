# Copy to .env.local. NEXT_PUBLIC_API_BASE_URL is required — the build fails without it.
NEXT_PUBLIC_API_BASE_URL=https://talim-be-dev.onrender.com
# Optional; defaults to the API origin.
NEXT_PUBLIC_WEBSOCKET_URL=
